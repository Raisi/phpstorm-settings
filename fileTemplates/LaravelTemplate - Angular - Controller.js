const angular      = require('angular'),
  ngModuleName = '$NAME',
  ngModule     = angular.module(ngModuleName, [])
;


ngModule.controller('$NAME', ["${DS}scope", function(${DS}cope) {
    const _controller = this;

    (function init() {
        
    }());
}];

module.exports = ngModuleName;
